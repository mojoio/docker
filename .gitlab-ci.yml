image: hosttoday/ht-docker-dbase
services:
  - docker:dind

stages:
  - build
  - test
  - release
  - trigger

before_script:
  - npm uninstall -g npmci
  - npm install -g npmci
  - npmci prepare docker


build:
  stage: build
  script:
    - npmci build docker
  tags:
    - lossless
    - priv
  

test:
  stage: test
  script:
    - npmci test docker
  only:
    - master
  tags:
    - lossless
    - priv

release:
  stage: release
  script:
    - npmci publish npm
  only:
    - master
  tags:
    - lossless
    - priv

trigger:
  stage: trigger
  script:
    - npmci trigger
  only:
    - master
  tags:
    - lossless
    - priv